# Secrets
.env
nginx/.htpasswd
cookies/

# Local dev override (gitignored so it is never restored to the server by git pull)
# Copy docker-compose.override.yml.example to docker-compose.override.yml for local dev.
docker-compose.override.yml

# Local management info (instance IDs, access details, session notes — not for the repo)
local_management/

# Local Claude helper scripts (machine-specific, not for the repo)
.claude_scripts/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Runtime data (Docker volumes)
media/
data/

# ah session data
.ah/

# Editor
.DS_Store
*.swp
